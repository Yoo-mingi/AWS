인스턴스 생성을 아마존이 제공하는 것으로 만들고, 보안그룹 네트워크 등등은 기존의 것을 활용하던가 Defult로 생성한다.

http등 필요한 서비스를 다운하고, 작동이 확인된 EC2 인스턴스를 우클릭후 Image and Templates를 선택하고 Create Image를 클릭

해당 이미지의 이름을 입력하고 완료하면 이미지가 만들어진다.
AMI란에 가면 해당 이미지가 완성되었는데, 만약 해당하는 이미지로 EC2 인스턴스를 만든다면 http가 설치된 인스턴스를 만들 수 있는 것이다.

